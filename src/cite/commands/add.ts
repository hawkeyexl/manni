/** `manni cite add`: mint one citation and write it to a page. */
import type { AddOptions, AddResult } from "../types.js";
import { notImplemented } from "../core/not-implemented.js";

export function runAdd(opts: AddOptions): Promise<AddResult> {
  return notImplemented("runAdd", opts);
}
