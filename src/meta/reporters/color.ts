/**
 * The color decision moved to the shared layer when the second tool needed
 * it. This module stays so nothing in meta has to change its imports.
 */
export { palette, shouldColor, type Colors } from "../../shared/color.js";
