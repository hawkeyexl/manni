/**
 * Writers for the formats a termbase leaves the docs in: TBX, SKOS, CSV, JSON,
 * and a Vale style.
 */
import type { TermWriter } from "../../types.js";

export const INTERCHANGE_WRITERS: readonly TermWriter[] = [];
