/**
 * The `docmeta` CLI entry point. The metadata tool lives under `src/meta/`;
 * this file is the bundle entry `dist/cli.js` is built from, and it runs
 * `main()` when executed directly via the guard inside `./meta/cli.ts`.
 */
export * from "./meta/cli.js";
