---
title: "manni:audience:1.0.0-proposal.2"
description: "The proposed audience vocabulary, covering who a page serves, which journeys it belongs to, and who may see it."
sidebar:
  order: 3
  label: "audience"
id: audience-vocabulary-proposal
type: proposal
keywords:
  - audience
  - personas
  - journeys
  - visibility
  - metadata
  - frontmatter
  - vocabulary
  - schema
language: en
source-of-truth:
  path: docs/proposals/0023/schemas/audience/1.0.0-proposal.2.json
  kind: json-schema
audiences:
  - schema-authors
  - documentation-engineers
  - content-strategists
intent: Understand the proposed audience vocabulary fields and how to specify who a page serves
visibility: public
lifecycle: draft
applies-to: manni-meta
related-pages: /manni/meta/proposals/frontmatter-vocabularies/
risks: read-only
sample-questions:
  - What fields does the audience vocabulary include?
  - How do I specify who a page is for?
  - What is the difference between audiences and personas?
  - When should I use visibility versus lifecycle?
meta-provenance:
  - generated-by: claude-sonnet-4-5
    fields:
      - /id
      - /type
      - /keywords
      - /language
      - /source-of-truth
      - /audiences
      - /intent
      - /visibility
      - /lifecycle
      - /applies-to
      - /related-pages
      - /risks
      - /sample-questions
    confidence:
      /id: 0.85
      /type: 0.95
      /keywords: 0.9
      /language: 1
      /source-of-truth: 0.95
      /audiences: 0.85
      /intent: 0.9
      /visibility: 0.95
      /lifecycle: 0.85
      /applies-to: 0.9
      /related-pages: 0.95
      /risks: 1
      /sample-questions: 0.85
---

:::caution[Proposal under review]
Part of the [metadata vocabularies proposal](/manni/meta/proposals/frontmatter-vocabularies/),
open for community review. Draft schema:
[`docs/proposals/0023/schemas/audience/1.0.0-proposal.2.json`](https://github.com/hawkeyexl/manni/blob/main/docs/proposals/0023/schemas/audience/1.0.0-proposal.2.json).
:::

**The question it answers: who does this page serve, and who may see it?**
`audiences` works in any repo from the start. `personas` and `journeys`
are the upgrade once a content strategy exists to point into.
`visibility` is the access switch that downstream tooling can act on.

## Fields

| Field | Type | Location | Notes |
|---|---|---|---|
| `audiences` | string or list | `page` | Who it is for, in your own words: `administrators`, `sre`. Plural because pages usually serve several groups. Not enumerated, because audience taxonomies belong to the org |
| `personas` | string or list | `external` | Ids into your content-strategy documents (`persona-platform-admin`). Adopt it once you keep personas; until then the ids would point at nothing |
| `journeys` | string or list | `external` | Ids of the user journeys (CUJs) this page belongs to. This is how a docs set shows it covers its own strategy |
| `intent` | string, non-empty | `page` | The reader's job, in one line: `deploy the operator on a running cluster`. Retrieval matches questions against it |
| `visibility` | enum | `external` | `draft` → `restricted` → `confidential` → `internal` → `public` |

The Location column is the `x-manni-location` mark each field carries in
`manni:audience:1.0.0-proposal.2`. `page` means the value belongs in the
document's own metadata and is meant to reach delivered output. `external`
means it belongs in the collection's external-metadata manifest. A field is
`page` when a third-party or user-owned agent fetching the page would act on
it. `audiences` and `intent` are what an ingester filters and routes on.
`personas` and `journeys` are internal names that mean nothing outside, and
`visibility` is a build-time switch, because delivered output is public by
definition. See [field location](/manni/meta/schemas/field-location/) for how
the marks are used.

## Example

```yaml
---
title: Install the operator on Kubernetes
description: Deploy the operator with Helm and verify the rollout.
audiences: [administrators]
personas: [persona-platform-admin]
journeys: [cuj-install]
intent: Deploy the operator on a running cluster
visibility: public
---
```

## Design decisions

- **`visibility` is enumerated; `audiences` is not.** Something downstream
  branches on `visibility`, and an unrecognized access level would fail
  open without anyone noticing. Audience taxonomies belong to the org, so
  `audiences` takes any label. The same reason produces both calls.
- **`visibility` folds a draft flag and an access level into one
  switch.** Your generator's own `draft` or `unlisted` keys remain its
  rendering controls, and this vocabulary does not claim them. A page can
  be `visibility: public` in metadata while the generator still hides it.
  Reconciling the two is a job for site tooling, not for a schema.
  `visibility` is also distinct from `lifecycle` (its own
  [vocabulary](/manni/meta/proposals/lifecycle/)). `lifecycle: draft` says
  the content is unfinished. `visibility: draft` says nobody outside the
  authors can see it. An unfinished page that is already visible inside
  the org is a legal and common combination.
- **There is no `expertise` field.** Reader level belongs to the persona
  definitions a page points at. A page saying `expertise: beginner` next
  to `personas: [persona-expert-admin]` could contradict the persona's
  own proficiency. This follows the family's principle that a fact
  belongs at the level that owns it.
- **`intent` is free text.** Most strings that nothing could enforce were
  cut. This one stays because machines consume it: retrieval and eval
  pipelines match reader questions against it.

## What reviewers should poke at

Two questions. Are the five stages of the `visibility` ladder the right
closed set for most orgs? And does keeping both `audiences` (labels) and
`personas` (strategy ids) read as an upgrade path or as duplication?
