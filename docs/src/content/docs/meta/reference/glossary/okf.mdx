---
title: OKF
description: What OKF is, and how its v0.1 schema anchors the set manni meta validates with when nothing else is configured.
type: term
id: okf
label: OKF
alt-labels: [Open Knowledge Format]
related-terms: [schema]
abstract: The format of the schema anchoring manni meta's default set.
definition: >-
  The Open Knowledge Format, whose v0.1 schema ships with manni meta and
  anchors its built-in default set.
keywords:
  - OKF
  - Open Knowledge Format
  - schema
  - metadata
  - validation
  - default schema
  - google:okf:0.1
language: en
source-of-truth: src/meta/schemas/okf/0.1.json
intent: understand what OKF is and how its schema anchors manni meta's default validation set
visibility: public
lifecycle: published
concepts:
  - OKF
  - schema
next-steps: /manni/meta/reference/okf-schema/
related-pages: /manni/meta/reference/okf-schema/
sample-questions:
  - What is OKF?
  - What does OKF require in metadata?
  - How does OKF relate to manni meta's default validation?
  - What is the schema ID for OKF?
meta-provenance:
  - generated-by: claude-sonnet-4-5
    fields:
      - /keywords
      - /language
      - /source-of-truth
      - /intent
      - /visibility
      - /lifecycle
      - /concepts
      - /next-steps
      - /related-pages
      - /sample-questions
    confidence:
      /keywords: 0.85
      /language: 0.95
      /source-of-truth: 0.85
      /intent: 0.85
      /visibility: 0.9
      /lifecycle: 0.9
      /concepts: 0.8
      /next-steps: 0.75
      /related-pages: 0.9
      /sample-questions: 0.8
---

<p>{frontmatter.definition}</p>

{/* cite okf-requires-only-type */}
The schema's id is `google:okf:0.1`. OKF requires only `type` and treats every
other field (`title`, `description`, `resource`, `tags`, `timestamp`) as
recommended, tolerating unknown keys. When nothing else is configured, it
validates your files alongside `passo-uno:seven-action:1.0`. See
[the built-in OKF schema](/manni/meta/reference/okf-schema/).
