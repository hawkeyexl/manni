---
title: Schema reference
description: What a schema reference is in manni meta, and the three kinds manni meta sorts one into by its shape.
type: term
id: schema-reference
label: schema reference
related-terms: [schema, $schema key]
abstract: A pointer to a schema, as a builtin id, file, or URL.
definition: >-
  A pointer to a schema.
keywords:
  - schema reference
  - schema resolution
  - builtin
  - file
  - URL
  - reference kinds
language: en
source-of-truth:
  file: src/meta/core/schema-registry.ts
intent: Understand what a schema reference is and how manni meta classifies it into three kinds.
concepts:
  - schema reference
  - schema
  - $schema key
related-pages:
  - /manni/meta/reference/schema-resolution/
risks: read-only
sample-questions:
  - What is a schema reference?
  - What are the three kinds of schema references in manni meta?
  - How does manni meta classify schema references?
alt-labels:
  - schema ref
  - reference to schema
meta-provenance:
  - generated-by: claude-sonnet-4-5
    fields:
      - /keywords
      - /language
      - /source-of-truth
      - /intent
      - /concepts
      - /related-pages
      - /risks
      - /sample-questions
      - /alt-labels
    confidence:
      /keywords: 0.9
      /language: 1
      /source-of-truth: 0.95
      /intent: 0.85
      /concepts: 0.9
      /related-pages: 1
      /risks: 1
      /sample-questions: 0.85
      /alt-labels: 0.7
---

<p>{frontmatter.definition}</p>

It is what appears in a `--schema` flag, a document's
[`$schema`](/manni/meta/reference/glossary/schema-key/) key, or config. manni
meta classifies each reference into one of three kinds by its shape:

- **builtin**: a `vendor:name:version` id such as `google:okf:0.1`, resolved to
  a schema shipped with manni meta.
- **file**: a `.json` path or any path with a separator, such as
  `./my.schema.json`, resolved from the local filesystem.
- **url**: an `http://` or `https://` URL, fetched over the network.

See [Reference kinds](/manni/meta/reference/schema-resolution/#reference-kinds).
