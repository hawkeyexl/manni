---
title: Extraction
description: What extraction is in manni meta, the get command that does it alone, and how it differs from validation.
type: term
id: extraction
label: extraction
related-terms: [validation, extractor]
abstract: Reading metadata out of a document.
definition: >-
  The operation that reads metadata out of a document.
keywords:
  - extraction
  - metadata
  - get command
  - validation
  - extractor
language: en
intent: Understand what extraction is in manni meta and how it differs from validation
visibility: public
lifecycle: published
applies-to: manni meta
concepts:
  - extraction
  - validation
  - extractor
related-pages:
  - /manni/meta/reference/glossary/validation/
  - /manni/meta/reference/cli/
risks: read-only
sample-questions:
  - What is extraction in manni meta?
  - How does extraction differ from validation?
  - What command performs extraction without validation?
meta-provenance:
  - generated-by: claude-sonnet-4-5
    fields:
      - /keywords
      - /language
      - /intent
      - /visibility
      - /lifecycle
      - /applies-to
      - /concepts
      - /related-pages
      - /risks
      - /sample-questions
    confidence:
      /keywords: 0.8
      /language: 0.95
      /intent: 0.85
      /visibility: 0.8
      /lifecycle: 0.85
      /applies-to: 0.9
      /concepts: 0.9
      /related-pages: 0.85
      /risks: 0.9
      /sample-questions: 0.85
---

<p>{frontmatter.definition}</p>

{/* cite get-validates-nothing */}
The `get` command prints named field values and validates nothing. Extraction
and [validation](/manni/meta/reference/glossary/validation/) are two distinct
operations, exposed as two commands. Every validation begins with an
extraction, but you can extract without validating. See the
[CLI reference](/manni/meta/reference/cli/).
