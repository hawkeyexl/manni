---
title: Metadata
description: What metadata means in manni meta, where it comes from, and what manni meta checks about it.
type: term
id: metadata
label: metadata
related-terms: [extractor, frontmatter]
abstract: The key/value pairs manni meta validates.
definition: >-
  The key/value pairs manni meta validates.
keywords:
  - key-value pairs
  - validation
  - frontmatter
  - extraction
  - format
  - presence
language: en
intent: Understand what metadata means in manni meta and how it is validated
visibility: public
lifecycle: published
concepts:
  - metadata
  - extractor
  - frontmatter
prerequisites:
  - extractor
  - frontmatter
sample-questions:
  - What is metadata in manni meta?
  - Where does metadata come from?
  - What does manni meta check about metadata?
alt-labels:
  - document metadata
  - frontmatter fields
  - key-value pairs
scope-note: In manni meta, metadata specifically refers to the generic key/value pairs extracted from document frontmatter, not the prose content.
narrower:
  - frontmatter
meta-provenance:
  - generated-by: claude-sonnet-4-5
    fields:
      - /keywords
      - /language
      - /intent
      - /visibility
      - /lifecycle
      - /concepts
      - /prerequisites
      - /sample-questions
      - /alt-labels
      - /scope-note
    confidence:
      /keywords: 0.85
      /language: 0.95
      /intent: 0.9
      /visibility: 0.9
      /lifecycle: 0.9
      /concepts: 0.85
      /prerequisites: 0.8
      /sample-questions: 0.85
      /alt-labels: 0.75
      /scope-note: 0.8
---

<p>{frontmatter.definition}</p>

{/* cite metadata-is-generic-key-values */}
They are fields such as `type`, `title`, and `timestamp`. Metadata is what an
[extractor](/manni/meta/reference/glossary/extractor/) pulls out of a
document's [frontmatter](/manni/meta/reference/glossary/frontmatter/).
Everything in manni meta after extraction operates on this generic key/value
shape, regardless of the original format. manni meta checks the **presence and
format** of metadata, never the document's prose.
