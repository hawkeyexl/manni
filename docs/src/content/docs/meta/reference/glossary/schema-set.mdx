---
title: Schema set
description: What a schema set is in manni meta, and why a file must satisfy every schema in it.
type: term
id: schema-set
label: schema set
related-terms: [schema]
abstract: The schemas resolved for one file, all of which apply.
definition: >-
  The one-or-more schemas resolved for a single file.
keywords:
  - schema set
  - validation
  - schema resolution
  - metadata validation
language: en
intent: understand what a schema set is and why a file must satisfy every schema in its set
visibility: public
lifecycle: published
concepts:
  - schema
prerequisites: schema
risks: read-only
sample-questions:
  - What is a schema set?
  - Why must a file satisfy every schema in its set?
meta-provenance:
  - generated-by: claude-sonnet-4-5
    fields:
      - /keywords
      - /language
      - /intent
      - /visibility
      - /lifecycle
      - /concepts
      - /prerequisites
      - /risks
      - /sample-questions
    confidence:
      /keywords: 0.85
      /language: 1
      /intent: 0.95
      /visibility: 0.9
      /lifecycle: 0.9
      /concepts: 0.95
      /prerequisites: 0.85
      /risks: 0.95
      /sample-questions: 0.9
---

<p>{frontmatter.definition}</p>

{/* cite every-schema-in-the-set-must-pass */}
manni meta resolves a schema **set** per file, then validates the file's
metadata against **every** [schema](/manni/meta/reference/glossary/schema/) in
it. A file passes only when it satisfies all schemas in its set, and each
reported error is tagged with the schema that produced it. See
[Schema resolution](/manni/meta/reference/schema-resolution/#validating-against-the-whole-set).
