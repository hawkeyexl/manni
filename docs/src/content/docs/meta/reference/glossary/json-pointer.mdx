---
title: JSON Pointer
description: What a JSON Pointer is, how manni meta uses one to name the field an error is about, and what an empty one means.
type: term
id: json-pointer
label: JSON Pointer
abstract: The syntax that names the field an error is about.
definition: >-
  The standard syntax manni meta uses to name the field an error is about.
keywords:
  - RFC 6901
  - instancePath
  - validation
  - error messages
  - JSON Schema
language: en
source-of-truth:
  url: https://datatracker.ietf.org/doc/html/rfc6901
  kind: rfc
intent: understand what JSON Pointer syntax means in validation error messages
concepts:
  - validation
sample-questions:
  - What is a JSON Pointer?
  - What does (root) mean in error output?
  - How do I interpret the instancePath field?
  - What does /tags/0 mean in a validation error?
meta-provenance:
  - generated-by: claude-sonnet-4-5
    fields:
      - /keywords
      - /language
      - /source-of-truth
      - /intent
      - /concepts
      - /sample-questions
    confidence:
      /keywords: 0.85
      /language: 0.95
      /source-of-truth: 0.92
      /intent: 0.9
      /concepts: 0.75
      /sample-questions: 0.88
---

<p>{frontmatter.definition}</p>

{/* cite empty-pointer-shown-as-root */}
The standard is [RFC 6901](https://datatracker.ietf.org/doc/html/rfc6901). A
pointer like `/tags/0` means "the first item of the `tags` array". An empty
pointer, shown as `(root)` in pretty output, means the document as a whole.
That is most often a required field that's missing entirely. Pointers appear in
error output and in the `instancePath` field of JSON output.
