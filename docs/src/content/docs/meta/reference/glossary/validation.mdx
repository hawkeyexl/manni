---
title: Validation
description: What validation is in manni meta, the validate command CI gates on, and how it differs from extraction.
type: term
id: validation
label: validation
related-terms: [extraction, schema set]
abstract: Checking extracted metadata against its schema set.
definition: >-
  The operation that checks that extracted metadata satisfies its schema set.
keywords:
  - validation
  - metadata
  - schema
  - extraction
  - validate command
  - CI
language: en
intent: Understand what validation is in manni meta and how it differs from extraction
concepts:
  - validation
  - extraction
  - schema set
prerequisites: extraction
related-pages:
  - extraction
  - /manni/meta/reference/cli/
sample-questions:
  - What is validation in manni meta?
  - How does validation differ from extraction?
  - What command does CI gate on for validation?
alt-labels:
  - validate
  - validation operation
meta-provenance:
  - generated-by: claude-sonnet-4-5
    fields:
      - /keywords
      - /language
      - /intent
      - /concepts
      - /prerequisites
      - /related-pages
      - /sample-questions
      - /alt-labels
    confidence:
      /keywords: 0.9
      /language: 1
      /intent: 0.95
      /concepts: 0.95
      /prerequisites: 0.85
      /related-pages: 0.9
      /sample-questions: 0.85
      /alt-labels: 0.75
---

<p>{frontmatter.definition}</p>

{/* cite validation-begins-with-extraction */}
That is the `validate` command, the one CI gates on. Validation and
[extraction](/manni/meta/reference/glossary/extraction/) are two distinct
operations, exposed as two commands. Every validation begins with an
extraction, but you can extract without validating. See the
[CLI reference](/manni/meta/reference/cli/).
