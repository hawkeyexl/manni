# Sidecar fixture (proposal 0037): the private half of a public docset.
meta:
  paths:
    - "docs/**/*.md"
  sidecars:
    - file: ./docs-meta.yaml
      keys: [source, jira]
  overrides:
    - files: "docs/**/*.md"
      schemas:
        - ./private.schema.json
