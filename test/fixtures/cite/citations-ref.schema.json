{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/manni/citations-ref.schema.json",
  "title": "A house schema that reaches the citations vocabulary through $ref",
  "description": "docs/src/content/docs/cite/set-up/require-citations.mdx says a house schema can reach the built-in citations vocabulary through $ref, because every built-in is registered for reference resolution. This file is that composition. A malformed entry fails under it, and the finding names this file rather than the vocabulary.",
  "type": "object",
  "allOf": [{ "$ref": "manni:citations:1.0.0" }],
  "required": ["citations"]
}
