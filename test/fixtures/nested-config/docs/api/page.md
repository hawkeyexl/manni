---
type: guide
title: Hi
description: A page that satisfies the built-in default set.
---

# Hi

Satisfies the built-in default schema set and violates the configured one, so
which config a run discovers decides the verdict.
