---
title: Bad graph block
graph:
  alt-labels: [orphaned]
  bogus: true
---

# Bad graph block

Two schema failures in one block: `alt-labels` without the `label` it describes,
and a key the `graph` block does not define.
