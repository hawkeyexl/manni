---
title: Rate limits
description: What the gateway does when a client goes over.
type: how-to
concepts: [throttling]
owner: platform-team
graph:
  label: Rate limiting
  alt-labels: [request caps]
---

# Rate limits

`concepts` and `owner` are marked `x-manni-kg-output: false`, so neither reaches
the graph. `graph.label` carries a nested mark, which is ignored.
