# Proves the SARIF and JUnit reporters against the real consumers, not just
# against unit tests.
#
# Green tests show the writers emit what we *think* the specs say. They cannot
# show GitHub accepting the upload, rendering the alerts, or — the one that
# matters — resolving `artifactLocation.uri` against the repository root. That
# last failure is silent: a wrong URI means the upload succeeds and code
# scanning shows **zero alerts**, with every job step green. Only a real upload
# catches it, which is why this workflow exists.
#
# `test/fixtures/infer/` is excluded. Those files exist to be *counted* by
# `manni meta schemas infer`, not validated, and one of them has no frontmatter at
# all — that is the whole point of it, since a coverage report has to say how
# many files carry no metadata block. Validating it here would add a standing
# code-scanning alert for a file that is doing its job, and fail the `manni`
# check on every PR that touched it.
#
# `test/fixtures/query/` is excluded for the same reason. Those files exist to
# be *queried* by `manni meta query` — one has no frontmatter at all so that
# `WHERE _present = 0` has something to find, and none carries a `type:`,
# because the query fixtures exercise retrieval, not any schema.
#
# `test/fixtures/query-ddl/`, `query-ddl-builtin/`, and `ddl-bridge/` are
# excluded with them. Their docs validate against the fixture's *own* config
# (the house schema, or a pinned builtin) inside the DDL tests; judged against
# this workflow's default set they are standing alerts for files doing their
# job.
#
# `test/fixtures/default-schema/` is excluded for the same reason. Those
# fixtures validate against the proposal-0023 draft vocabularies via file refs
# in `test/default-schema.test.ts`, and several are deliberately invalid for
# *those* drafts; judged against this workflow's default set they are standing
# alerts for files doing their job.
#
# `test/fixtures/checks/` is excluded with them. Its docs validate against the
# fixture's *own* permissive config inside the corpus-check tests (0026) —
# their job is to carry a duplicate slug, not a `type:`; judged against this
# workflow's default set they are standing alerts for files doing their job.
# `test/fixtures/query-csv/` and `test/fixtures/collections/` are excluded
# ahead of the fixtures 0029 and 0027 add for the same reason — a glob that
# matches nothing is harmless. `test/fixtures/checks-collections/` and
# `test/fixtures/external-metadata-overlap/` (0041) are excluded on the same
# principle: each carries its own `manni.config.yaml`, and its pages are
# shaped for what that config tests — a `FROM <collection>` check, a file in
# two collections — not for the default schema set.
#
# `test/fixtures/platform/` is excluded on the same principle, and it is the
# clearest case of it. Every file there exists to be judged against the platform
# schema it is named for — Starlight, Antora, Hugo, Jekyll, VitePress, MkDocs.
# Those are *platform* schemas, which by design require only what the generator
# refuses to build without, and no generator requires `type:`. So judged against
# this workflow's default set, every one of them is a standing alert for a file
# doing its job.
#
# They went unnoticed until MkDocs landed only because this workflow does not
# run on every push — it triggers on reporter changes, so a PR adding a platform
# fixture without touching a reporter never surfaced them. The MkDocs PR did
# both at once, and eleven alerts arrived together.
#
# `test/fixtures/infer-parity/` is excluded for the reason `test/fixtures/infer/`
# already is: those files exist to be *counted* by `manni meta schemas infer`, one
# of them deliberately carrying a key the other does not.
#
# `test/fixtures/get-parse-error/` is deliberately NOT excluded. Its unparseable
# file is a real parse failure, which is exactly the explainable alert this
# workflow exists to demonstrate — the same standing as `dd/malformed-yaml.md`.
#
# It runs over `test/fixtures/`, which already holds deliberately-invalid
# documents (missing-type.md, bad-timestamp.md, dd/malformed-yaml.md, …). Those
# are committed, reproducible, and already the material the docs' worked
# examples use — better than synthesised failures, because every alert here is
# explainable.
#
# Deliberately NOT on every push. It runs when the reporters change and on
# demand, so it stays a live demonstration and a regression check without
# accumulating standing alerts in the Security tab.
name: Formats demo

on:
  pull_request:
    paths:
      - "src/meta/reporters/**"
      - "src/meta/core/baseline.ts"
      - ".github/workflows/formats-demo.yml"
  workflow_dispatch:

permissions:
  contents: read
  # Required by upload-sarif. Nothing here needs write access to anything else.
  security-events: write

jobs:
  formats-demo:
    name: SARIF to code scanning, JUnit to the run summary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run build

      # `continue-on-error`, because validate exits 1 when there are findings
      # and the findings ARE the point. Without it the upload step never runs,
      # precisely in the case worth uploading.
      - name: Validate fixtures as SARIF
        id: sarif
        continue-on-error: true
        run: |
          node dist/cli.js meta validate "test/fixtures/**/*.md" \
            --exclude "test/fixtures/infer/**" \
            --exclude "test/fixtures/default-schema/**" \
            --exclude "test/fixtures/query/**" \
            --exclude "test/fixtures/query-ddl/**" \
            --exclude "test/fixtures/query-ddl-builtin/**" \
            --exclude "test/fixtures/ddl-bridge/**" \
            --exclude "test/fixtures/checks/**" \
            --exclude "test/fixtures/query-csv/**" \
            --exclude "test/fixtures/collections/**" \
            --exclude "test/fixtures/platform/**" \
            --exclude "test/fixtures/infer-parity/**" \
            --exclude "test/fixtures/external-metadata/**" \
            --exclude "test/fixtures/external-metadata-join/**" \
            --exclude "test/fixtures/external-metadata-overlap/**" \
            --exclude "test/fixtures/checks-collections/**" \
            --exclude "test/fixtures/cite/**" \
            --allow-empty -f sarif > manni.sarif
          echo "results=$(jq '.runs[0].results | length' manni.sarif)" >> "$GITHUB_OUTPUT"

      # stdout must be pure JSON: manni sends diagnostics to stderr for every
      # machine format, and a single leaked line here would make the upload
      # below reject the file while this job stayed green.
      - name: Check the SARIF is well-formed before uploading
        run: |
          jq -e '.version == "2.1.0" and (.runs | length) > 0' manni.sarif > /dev/null
          echo "SARIF is valid JSON with ${{ steps.sarif.outputs.results }} result(s)."
          echo "Artifact URIs (must be repository-root-relative, or code scanning silently drops them):"
          jq -r '.runs[0].results[].locations[0].physicalLocation.artifactLocation.uri' manni.sarif | sort -u

      - name: Upload SARIF to code scanning
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: manni.sarif
          # Namespaces these alerts so they are plainly a fixture demonstration
          # and cannot be mistaken for a scan of the shipped source.
          category: manni-fixtures

      - name: Validate fixtures as JUnit
        continue-on-error: true
        run: |
          node dist/cli.js meta validate "test/fixtures/**/*.md" \
            --exclude "test/fixtures/infer/**" \
            --exclude "test/fixtures/default-schema/**" \
            --exclude "test/fixtures/query/**" \
            --exclude "test/fixtures/query-ddl/**" \
            --exclude "test/fixtures/query-ddl-builtin/**" \
            --exclude "test/fixtures/ddl-bridge/**" \
            --exclude "test/fixtures/checks/**" \
            --exclude "test/fixtures/query-csv/**" \
            --exclude "test/fixtures/collections/**" \
            --exclude "test/fixtures/platform/**" \
            --exclude "test/fixtures/infer-parity/**" \
            --exclude "test/fixtures/external-metadata/**" \
            --exclude "test/fixtures/external-metadata-join/**" \
            --exclude "test/fixtures/external-metadata-overlap/**" \
            --exclude "test/fixtures/checks-collections/**" \
            --allow-empty -f junit > manni-junit.xml

      # GitHub renders no JUnit natively. A third-party reporter action would be
      # prettier; a step summary costs no new supply-chain surface in a repo
      # whose release pipeline already runs with elevated permissions.
      - name: Render JUnit to the run summary
        if: always()
        run: |
          {
            echo "## manni JUnit output"
            echo
            echo "| Metric | Value |"
            echo "|---|---|"
            for k in tests failures errors; do
              v=$(sed -n "s/.*<testsuites[^>]*$k=\"\([0-9]*\)\".*/\1/p" manni-junit.xml | head -1)
              echo "| $k | ${v:-0} |"
            done
            echo
            echo "<details><summary>Failing test cases</summary>"
            echo
            echo '```xml'
            # `|| true`: bash -e would fail the whole step on grep exit 1,
            # i.e. precisely when the fixtures are clean and there is nothing
            # to report. A clean run must still produce a summary.
            grep -B 1 "<failure" manni-junit.xml | head -40 || true
            echo '```'
            echo
            echo "</details>"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload both reports as artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: manni-formats
          path: |
            manni.sarif
            manni-junit.xml
